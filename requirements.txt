# Django 5.2+ (LTS-friendly)
Django>=5.2,<6.1
# PostgreSQL adapter (psycopg3)
psycopg[binary]>=3.1,<3.2
# Environment and secrets (12-factor)
python-dotenv>=1.0,<1.1
# ASGI server (for production)
uvicorn[standard]>=0.30,<0.31
# WSGI server
gunicorn>=23.0,<24.0
# Django extensions
django-extensions>=3.2,<3.3
# Better interactive shell for `shell_plus --ipython`
ipython>=8.26,<9.0
